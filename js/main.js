 // # Raccolta Dati 
const firstUserAge = parseInt(prompt("Età del passeggero:"));
const distance = parseInt(prompt("Distanza in km"));
const priceAtKm = 0.21;     // prezzo al km
const price = distance * priceAtKm;     // prezzo totale del viaggio
const discountPerc1 = 20;   // scotnto per i minorenni
const discountPerc2 = 40;  // sconto per gli over.65
const discountAmount = price * discountPerc1 / 100; // sconto per i minorenni
const discountAmount2 = price * discountPerc2 / 100; // sconto per gli over.65

console.log(firstUserAge);
console.log(distance);

// # SVOLGIMENTO
if (conditions) {
    
}
// OUTPUT


